# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["master"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
- name: ScrapingAnt Web Scrape
  # You may pin to the exact commit or the version.
  # uses: ScrapingAnt/scrapingant-scrape-action@fc517b56d3ff6fdac8093845d7197b7f522f6c59
  uses: ScrapingAnt/scrapingant-scrape-action@v1.0.0
  with:
    # ScrapingAnt API key. Get free at https://app.scrapingant.com/signup
    api-key: 
    # URL to scrape
    url: 
    # Response type: html, markdown, or extract
    output-type: # optional, default is html
    # For output-type=extract only. Comma-separated data fields to extract, e.g. "product title, price, description"
    extract-properties: # optional, default is 
    # Enable headless Chrome JS rendering (true/false)
    browser: # optional, default is true
    # Proxy type: datacenter or residential
    proxy-type: # optional, default is datacenter
    # Two-letter country code for geo-targeting (e.g. us, uk, de)
    proxy-country: # optional, default is 
    # Max request time in seconds (5-60)
    timeout: # optional, default is 60
    # Optional file path to save output (e.g. output.html, data.json)
    output-file: # optional, default is 
